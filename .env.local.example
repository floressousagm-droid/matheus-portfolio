# Copie este arquivo para .env.local (localmente) ou configure a mesma
# variável nas Environment Variables do projeto na Vercel.

# --- Formulário de contato (Resend) ---
# Única variável necessária. Veja README.md — seção "Formulário de contato".
RESEND_API_KEY=

# O painel de edição (/keystatic) não precisa de configuração nenhuma:
# roda apenas em desenvolvimento e grava direto nos arquivos do projeto.
